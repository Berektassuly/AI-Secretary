# AI Meeting Secretary

> Mission-critical система для автоматизации протоколирования встреч: транскрибация через OpenAI Whisper API, извлечение задач и их публикация в Jira.

## 🧭 Архитектурный обзор

Платформа построена по принципам zero-trust и микросервисной архитектуры. UI никогда напрямую не обращается к внешним API — все вызовы проходят через backend-for-frontend (Next.js API Routes).

```
[Браузер]
   ↓
[Next.js App Router]
   ├── POST /api/ingest/zoom ──▶  Zoom Cloud Recordings API
   ├── POST /api/ingest/google-meet ──▶  Google Drive API (Meet recordings)
   ├── POST /api/whisper  ──▶  OpenAI Whisper API
   ├── POST /api/extract-tasks ──▶  Python NLP Microservice
   └── POST /api/create-jira-tasks ──▶  Jira Cloud API

[Python FastAPI NLP Service]
   └── POST /extract-tasks
```

### Сервисы

| Сервис | Стек | Назначение |
| ------ | ---- | ---------- |
| `ai-secretary-app` | Next.js 14 (App Router), TypeScript, Tailwind CSS | UI, оркестрация процесса, безопасные вызовы внешних API |
| `nlp_service` | FastAPI, Transformers | CPU/GPU-bound анализ текста, извлечение actionable-задач |

## 🚀 Быстрый старт

### 1. Подготовка окружения

1. Установите Docker и Docker Compose.
2. Скопируйте `.env` файлы на основе примеров:
   - `cp ai-secretary-app/.env.local.example ai-secretary-app/.env.local`
   - `cp nlp_service/.env.example nlp_service/.env`
3. В `ai-secretary-app/.env.local` пропишите ключ OpenAI (`OPENAI_API_KEY`) и при необходимости настройте модель/таймауты для транскрипции.
4. Дополнительно укажите `NEXT_PUBLIC_JIRA_DEFAULT_BASE_URL` и параметры NLP сервиса, если они отличаются от дефолтных.

### 2. Запуск всей платформы

```bash
docker compose build
docker compose up
```

После старта:
- Next.js приложение доступно на `http://localhost:3000`.
- NLP сервис — на `http://localhost:8000` (должен быть недоступен напрямую пользователю, в production спрячьте его за сетью сервисов).

### 3. Локальная разработка без Docker

- **Next.js**:
  ```bash
  cd ai-secretary-app
  npm install
  npm run dev
  ```
- **NLP Service**:
  ```bash
  cd nlp_service
  python -m venv .venv
  source .venv/bin/activate
  pip install -r requirements.txt
  uvicorn app.main:app --host 0.0.0.0 --port 8000
  ```

Убедитесь, что переменные окружения заданы (можно использовать `.env.local` и `.env`).

## 📝 Сценарий создания заметок и задач

1. **Перед встречей.** Настройте интеграцию с Jira (email, API token, project key) в разделе «Интеграция с Jira» — данные сохраняются только на время сессии.
2. **Загрузка записи.** После митинга загрузите аудио/видео в блок «Загрузите файл встречи». Компонент сразу проверит лимит в 25 МБ.
3. **Транскрибация.** UI показывает прогресс «Транскрипция», пока `/api/whisper` отправляет файл в OpenAI. В случае ошибок текст всплывает в статус-баре.
4. **Проверка задач.** После обработки NLP блок «Результаты» покажет список задач. Их можно скопировать целиком или отдельными пунктами (кнопка Copy).
5. **Создание Jira-тикетов.** Убедитесь, что итоговый список корректен, и нажмите «Создать задачи в Jira». Каждый пункт получит статус (успех/ошибка) с номером issue или текстом ошибки.
6. **Протоколирование.** Скопируйте транскрипцию и задачи в корпоративный документ или Confluence. Добавьте summary вручную — шаблон: цель встречи → принятые решения → задачи.
7. **История встреч.** В конце сессии панель «История встреч» автоматически сохранит транскрипт, извлечённые задачи и статус интеграции с Jira. История хранится локально и помогает отслеживать контекст нескольких созвонов подряд.

> Совет: если требуется просто конспект без Jira, достаточно выполнить шаги 2–4 и экспортировать список задач, например, в Notion/Google Docs.

## 🌐 Автоимпорт записей Zoom и Google Meet

Платформа умеет работать напрямую с облачными записями — не нужно вручную скачивать и загружать файлы.

| Источник | Что требуется | Как работает |
| -------- | ------------- | ------------ |
| **Zoom** | OAuth access token с областью `recording:read`, идентификатор встречи/UUID, при необходимости пароль записи | Эндпоинт `/api/ingest/zoom` запрашивает метаданные записей, скачивает нужный трек (audio/video) и передаёт его в Whisper → NLP → Jira. |
| **Google Meet** | OAuth токен Google с `drive.readonly`, идентификатор файла в Google Drive | Эндпоинт `/api/ingest/google-meet` читает запись из Google Drive, после чего следует тот же пайплайн. |

В интерфейсе это отражено компонентом `MeetingSourceTabs`: выберите вкладку Zoom/Google, укажите токен и идентификатор — остальное система сделает автоматически. Все чувствительные данные обрабатываются на сервере и не попадают в браузерную консоль.

## 🧠 Память встреч и повторное использование контекста

- Компонент `MeetingHistoryPanel` хранит до 10 последних обработанных митингов в `localStorage` пользователя: транскрипт, структурированные задачи и результат синхронизации с Jira.
- История пополняется автоматически при завершении распознавания (для загрузки файла, Zoom и Google Meet). Для каждого источника сохраняются метаданные (например, `meetingId` или название файла).
- При повторном открытии приложения история подхватывается хуком `useWorkflowManager` и доступна для копирования/повторной отправки задач. Очистка истории выполняется одним кликом и не требует обращения к серверу.

## ⚙️ Сервисы подробно

### Next.js (ai-secretary-app)

- **API Routes**
- `POST /api/ingest/zoom` — подключает облачную запись Zoom (OAuth токен + meetingId), скачивает аудио и прогоняет его через полный пайплайн.
- `POST /api/ingest/google-meet` — подтягивает запись из Google Drive (Meet), требует OAuth токен с `drive.readonly`.
- `POST /api/whisper` — принимает файл (FormData `file`), проверяет лимит 25 МБ и отправляет его в OpenAI Whisper API (поддерживаются кастомные base URL и таймауты). При отсутствии ключа возвращает понятную ошибку.
  - `POST /api/extract-tasks` — защищённый прокси к `nlp_service`. Обрабатывает таймауты и повторные попытки.
  - `POST /api/create-jira-tasks` — создаёт задачи в Jira. Накапливает результаты по каждой задаче.
- **UI/UX**
  - Переключатель источников встречи (`MeetingSourceTabs`) — загрузка файла, импорт из Zoom или Google Meet.
  - Проактивный прогресс бар (`WorkflowTracker`).
  - Дашборд результатов (`ResultsDashboard`) с полной метаинформацией (дедлайны, приоритеты, метки, уверенность модели).
  - Обновлённая форма интеграции с Jira (`JiraIntegrationForm`) с управлением приоритетами, дедлайнами и ярлыками.
  - Панель истории встреч (`MeetingHistoryPanel`) — локальная «память» агента: сохраняет до 10 сессий, поддерживает копирование задач и отображает результаты интеграции с Jira.
  - Хук `useWorkflowManager` инкапсулирует весь оркестратор состояний, работу с новыми источниками и синхронизирует локальную память встреч.

### Python NLP Service

- Эндпоинт `POST /extract-tasks` получает текст и возвращает список задач.
- Логика в `logic.py` использует предобученные NLI модели (`ruBERT`, `BART-MNLI`).
- Обогащение мета-данных выполняется `LLMTaskEnricher`, который по умолчанию запускает открытый TinyLlama (можно переопределить через `OPEN_SOURCE_LLM_ID`). При недоступности модели используются эвристики по ключевым словам и датам.
- Модели загружаются один раз при старте (через `FastAPI lifespan`).
- Встроенный health-check `GET /health`.

#### Как работает извлечение задач

1. **Определение языка.** Функция `detect_lang_code` считает русские и латинские символы, чтобы выбрать русскую или английскую модель.
2. **Предобработка текста.** Транскрипт разбивается на предложения и короткие фразы, объединения типа «и/and» делятся на отдельные действия.
3. **Грубый фильтр по глаголам.** Регулярное выражение `VERB_RE` ищет глаголы-действия (провести, отправить, plan, deploy и т. д.) и отбрасывает бытовые фразы.
4. **Нормализация.** Хвосты вроде «нужно», «давайте» и лишние знаки препинания удаляются, длинные фразы усекаются до 16 слов.
5. **ML-проверка.** Для каждого кандидата вызывается NLI модель. Если вероятность entailment ≥ 0.6, фраза сохраняется. Если модель не загрузилась, действует эвристика «разрешить».
6. **Дедупликация.** Задачи приводятся к списку без повторений с сохранением порядка появления.

#### Ручная проверка сервиса

```bash
curl -X POST http://localhost:8000/extract-tasks \
  -H "Content-Type: application/json" \
  -d '{"text": "Давайте подготовим отчет к пятнице и созвонимся с клиентом завтра."}'
```

В ответ придет `{"tasks": ["подготовим отчет к пятнице", "созвонимся с клиентом завтра"]}`. Если сервис не поднят или нет моделей, FastAPI вернет понятную ошибку с описанием.

## 🔐 Переменные окружения

### `ai-secretary-app`

| Переменная | Назначение |
| ---------- | ---------- |
| `OPENAI_API_KEY` | Ключ OpenAI для вызова Whisper API (обязателен). |
| `OPENAI_BASE_URL` | Кастомный base URL (Azure/OpenAI proxy), по умолчанию `https://api.openai.com/v1`. |
| `OPENAI_WHISPER_MODEL` | Имя модели для транскрипции (по умолчанию `whisper-1`). |
| `OPENAI_WHISPER_LANGUAGE` | Опциональная подсказка языка (ISO 639-1, например `ru`). |
| `OPENAI_ORGANIZATION` | Идентификатор организации OpenAI (опционально). |
| `OPENAI_PROJECT` | Идентификатор проекта OpenAI (опционально). |
| `OPENAI_REQUEST_TIMEOUT_MS` | Таймаут запроса к OpenAI Whisper (мс). |
| `NLP_SERVICE_URL` | URL Python сервиса (по умолчанию `http://nlp-service:8000`). |
| `NLP_SERVICE_REQUEST_TIMEOUT_MS` | Таймаут при обращении к NLP (45 сек). |
| `NLP_SERVICE_RETRY_COUNT` | Количество повторных попыток при ошибках NLP (2). |
| `JIRA_REQUEST_TIMEOUT_MS` | Таймаут запросов к Jira (45 сек). |
| `NEXT_PUBLIC_JIRA_DEFAULT_BASE_URL` | Базовый URL Jira, отображаемый в UI. |

### `nlp_service`

| Переменная | Назначение |
| ---------- | ---------- |
| `LOG_LEVEL` | Уровень логирования (по умолчанию `INFO`). |
| `RU_NLI_MODEL_DIR` / `RU_NLI_MODEL_NAME` | Кастомный путь/имя русской NLI модели. |
| `EN_NLI_MODEL_DIR` / `EN_NLI_MODEL_NAME` | Кастомный путь/имя английской NLI модели. |
| `OPEN_SOURCE_LLM_ID` | Идентификатор открытого LLM в Hugging Face (`TinyLlama/TinyLlama-1.1B-Chat-v1.0` по умолчанию). |
| `OPEN_SOURCE_LLM_DIR` | Локальная папка с моделью (используется вместо скачивания из сети). |
| `OPEN_SOURCE_LLM_DISABLED` | Установите `1`, чтобы полностью отключить LLM и использовать только эвристики. |
| `OPEN_SOURCE_LLM_MAX_NEW_TOKENS` | Сколько токенов LLM может сгенерировать для JSON-ответа (по умолчанию 256). |
| `OPEN_SOURCE_LLM_TEMPERATURE` | Температура генерации (0 — детерминированный ответ). |

## 📡 Контракты API

### `POST /api/whisper`
- Request: `multipart/form-data` с полем `file`.
- Response: `200 OK` → `{ "transcript": "..." }`.
- Ошибки: `400` (нет файла), `413` (лимит), `502` (ошибка от OpenAI Whisper API).

### `POST /api/extract-tasks`
- Request: `{ "transcript": "..." }`.
- Response: `{ "tasks": [{ "summary": "...", "assignee": "...", "due": "2024-05-10", "priority": "High", "labels": ["dev"], "confidence": 0.82 }] }`.
- Ошибки: `400` (валидация), `502` (ошибка NLP сервиса), `500` (непредвиденная ошибка).

### `POST /api/create-jira-tasks`
- Request: `{ "tasks": [{ "summary": "...", "assignee": "...", "due": "...", "priority": "...", "labels": ["..."] }], "config": { "baseUrl": "...", "email": "...", "token": "...", "projectKey": "...", "issueType"?: "Task", "defaultAssignee"?: "...", "defaultPriority"?: "...", "defaultLabels"?: ["..."], "description"?: "...", "syncAssignee"?: true, ... } }`.
- Response: `{ "results": [{ "summary": "...", "success": true, "issueKey": "AI-123", "issueUrl": "https://..." }] }`.
- Ошибки: `400` (валидация), `502` (все задачи упали), поле `error` внутри `results` укажет причину.

### `POST /api/ingest/zoom`
- Request: `{ "meetingId": "...", "accessToken": "...", "recordingType"?: "audio_only", "passcode"?: "..." }`.
- Response: `{ "transcript": "...", "tasks": [{ "summary": "...", "due": "..." }] }`.
- Ошибки: `400` (валидация), `502` (ошибка запроса к Zoom или скачивания записи).

### `POST /api/ingest/google-meet`
- Request: `{ "fileId": "...", "accessToken": "..." }`.
- Response: `{ "transcript": "...", "tasks": [...] }`.
- Ошибки: `400` (валидация), `502` (ошибка Google Drive / скачивания записи).

## 📊 Метрики качества

В репозитории есть мини-набор из реальных сценариев (`nlp_service/evaluation/dataset.json`) и скрипт `python evaluation/run_metrics.py`. Он запускает `TaskExtractor` (с отключённым LLM в целях детерминизма) и считает метрики по извлечению задач:

```bash
cd nlp_service
python evaluation/run_metrics.py
```

Средние показатели на датасете (TinyLlama отключена, работают эвристики + NLI):

| Метрика | Значение |
| ------- | -------- |
| Precision / Recall / F1 по summary | `0.875 / 0.875 / 0.875` |
| Точность определения приоритета | `0.42` |
| Точность меток | `0.13` |

Когда LLM доступна (см. переменные `OPEN_SOURCE_LLM_*`), качество по мета-данным улучшается, что отражается и в UX (указание исполнителя, дедлайна, ярлыков для Jira).

### `POST /extract-tasks` (NLP сервис)
- Request: `{ "text": "..." }`.
- Response: `{ "tasks": [{ "summary": "...", "assignee": "...", "due": "...", "labels": ["..."] }] }`.
- Ошибки: `500` (ошибка извлечения).

## 🧪 Тестирование и качество

- TypeScript strict mode включён.
- ESLint (`npm run lint`) и `uvicorn`/`fastapi` конфигурации адаптированы для production.
- Docker образы многоступенчатые и запускают приложение от непривилегированных пользователей.

## 🔒 Безопасность

- Аудиофайлы и Jira‑креды никогда не попадают на клиент — все вызовы идут через серверные роуты.
- NLP сервис изолирован, не хранит секреты и обслуживает только текстовый анализ.
- Встроены таймауты и повторные попытки, исключения логируются структурировано.

## 📄 Лицензия

Проект предоставляется «как есть» в рамках задания. Используйте и расширяйте его с учётом корпоративных стандартов безопасности.
